<div class="wrapper">
  <div class="">
    <%= render 'shared/navbar' %>
    <%= render 'shared/summary_page' %>
  </div>
  <div>
    <% @order.item_orders.each do |item_order| %>
      <%= render 'order_card', item_order: item_order, table: @table %>
    <% end %>
  </div>

  <div class="m-4">
    <section class="payment-btn-container">
      <%= link_to "Proceed to payment", table_checkout_path, class: 'payment-buttons'%>
    </section>
  </div>

  <section class="checkout-footer m-4">
    <h2><strong>Total:</strong></h2>
    <h2 id="total-price"><strong><%= @all_items_total_price %> €</strong></h2>
  </section>

</div>
